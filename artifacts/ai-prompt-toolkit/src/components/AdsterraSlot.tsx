import { useEffect, useRef } from "react";

type AdsterraSlotProps = {
  variant: "A" | "B";
  layout?: "desktop" | "mobile" | "auto";
};

export default function AdsterraSlot({ variant, layout }: AdsterraSlotProps) {
  // Adsterra ads disabled for AdSense approval
  return null;
}
